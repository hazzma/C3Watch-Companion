---
description: Run the Project Setup Agent to build the foundations, theme, and navigation of the Flutter Smartwatch App.
---

1.  Set up the Flutter project configuration and dependencies in `pubspec.yaml`.
2.  Define global constants and color palette in `lib/core/constants/`.
3.  Establish the dark theme design system in `lib/core/theme/app_theme.dart`.
4.  Implement the shell for the application including navigation and routing in `lib/main.dart` and `lib/app.dart`.
5.  Create visuals-only placeholder screens for all 5 major sections to visualize the final layout.
6.  Ensure the "BLE Status Pill" (APP-005) is present across all screens.

// turbo
7. Build the foundations using the "Project Setup Agent" persona.
8. Verify the folder structure follows FSD v1.0 specifications.
9. Verify the app compiles and navigates correctly through placeholders.
